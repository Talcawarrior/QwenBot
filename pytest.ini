[pytest]
testpaths = tests
python_files = test_*.py
# Suppress noisy SQLAlchemy 2.0 deprecation warnings (database/models.py
# uses the legacy declarative_base() pattern; not a blocker for paper mode).
filterwarnings =
    ignore::sqlalchemy.exc.MovedIn20Warning
    ignore::PendingDeprecationWarning