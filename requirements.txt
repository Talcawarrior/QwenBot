# QwenBot - Ultimate Hybrid Weather Betting Bot
# Python 3.12+

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Async HTTP Client
aiohttp>=3.9.0

# HTTP Client (used by scrapers/meteo.py, scrapers/polymarket.py,
# executor/settler.py for blocking HTTP calls; the bulk of new I/O
# uses aiohttp, but these legacy modules still import requests).
requests>=2.31.0

# WebSocket
websockets>=12.0

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# Data Processing
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.11.0  # For accurate Normal CDF (Abramowitz & Stegun approximation yerine)

# Utilities
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Logging & Monitoring
python-json-logger>=2.0.0

# Testing (Optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Code Quality (Optional)
ruff>=0.1.0

# py-clob_client intentionally NOT included here.
# Paper-mode safety: the bot uses DRY_RUN=true (default) and never
# submits real orders.  Including py-clob-client would risk accidental
# real-money transactions during development if DRY_RUN is ever unset.
# If you need live Polymarket trading, install from requirements-live.txt.

